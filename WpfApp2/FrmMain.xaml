<Window x:Class="WpfApp2.FrmMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2" WindowStartupLocation="CenterScreen"
        xmlns:vm="clr-namespace:WpfApp2.ViewModel"
        mc:Ignorable="d" DataContext="{Binding Source={StaticResource Locator},Path=Main}"
        Title="FrmMain" Height="316.379" Width="432.759">
    <Window.Resources>
        
    </Window.Resources>
    <Border Background="{StaticResource MainBrush}">
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Height="50"/>
            <Border DockPanel.Dock="Left">
                <StackPanel>
                    <ContentControl Content="{Binding ShortCutCommand}"
                                    ContentTemplate="{StaticResource NavigationCommand}">
                    </ContentControl>
                    <ContentControl >
                        
                    </ContentControl>
                </StackPanel>
            </Border>
            <Border>
                
            </Border>
        </DockPanel>
    </Border>
</Window>
